# Scheme Portal Application

## Commands to pull migrations on production

npx prisma db pull
npx prisma generate

## for cache clear

cd "C:/Users/hp/OneDrive/Desktop/riyasat-folders/scheme-application-portal" && rm -rf .next && npm run build 2>&1 | tail -80
